import type { NextPage } from 'next';
import { useRouter } from 'next/router';
const Id: NextPage = () => {
    const router = useRouter();
    return (
        <div>ブログ</div>
    );
};
export default Id;